[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn server:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[variables]
PORT = "8000"
# MONGO_URL, DB_NAME and EMERGENT_LLM_KEY will be set via Railway dashboard / CLI
